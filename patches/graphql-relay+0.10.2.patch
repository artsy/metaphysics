--- a/node_modules/graphql-relay/connection/arrayConnection.d.ts	1985-10-26 09:15:00
+++ b/node_modules/graphql-relay/connection/arrayConnection.d.ts	2026-04-23 11:22:03
@@ -1,3 +1,4 @@
+import type { GraphQLFieldResolver } from 'graphql';
 import type {
   Connection,
   ConnectionArguments,
@@ -6,6 +7,8 @@
 interface ArraySliceMetaInfo {
   sliceStart: number;
   arrayLength: number;
+  useValueAsEdge?: boolean;
+  resolveNode?: GraphQLFieldResolver<any, any> | null;
 }
 /**
  * A simple function that accepts an array and connection arguments, and returns
--- a/node_modules/graphql-relay/connection/arrayConnection.js	1985-10-26 09:15:00
+++ b/node_modules/graphql-relay/connection/arrayConnection.js	2026-04-23 11:20:55
@@ -73,10 +73,23 @@
     startOffset - sliceStart,
     endOffset - sliceStart,
   );
-  const edges = slice.map((value, index) => ({
-    cursor: offsetToCursor(startOffset + index),
-    node: value,
-  }));
+  const edges = slice.map((value, index) => {
+    const newEdge = meta.useValueAsEdge
+      ? {
+          ...value,
+          cursor: offsetToCursor(startOffset + index),
+        }
+      : {
+          cursor: offsetToCursor(startOffset + index),
+          node: !!meta.resolveNode ? meta.resolveNode(value) : value,
+        };
+
+    if (meta.resolveNode) {
+      return { ...value, ...newEdge };
+    }
+
+    return newEdge;
+  });
   const firstEdge = edges[0];
   const lastEdge = edges[edges.length - 1];
   const lowerBound = after != null ? afterOffset + 1 : 0;
--- a/node_modules/graphql-relay/connection/connection.d.ts	1985-10-26 09:15:00
+++ b/node_modules/graphql-relay/connection/connection.d.ts	2026-04-23 11:21:35
@@ -4,8 +4,10 @@
   GraphQLFieldConfigArgumentMap,
   GraphQLFieldConfig,
   GraphQLFieldResolver,
+  GraphQLInterfaceType,
   ThunkObjMap,
 } from 'graphql';
+export declare const PageInfoType: GraphQLObjectType<any, any>;
 /**
  * Returns a GraphQLFieldConfigArgumentMap appropriate to include on a field
  * whose return type is a connection type with forward pagination.
@@ -39,8 +41,11 @@
   nodeType: GraphQLNamedOutputType | GraphQLNonNull<GraphQLNamedOutputType>;
   resolveNode?: GraphQLFieldResolver<any, any>;
   resolveCursor?: GraphQLFieldResolver<any, any>;
+  edgeType?: GraphQLObjectType | null;
   edgeFields?: ThunkObjMap<GraphQLFieldConfig<any, any>>;
+  edgeInterfaces?: GraphQLInterfaceType[];
   connectionFields?: ThunkObjMap<GraphQLFieldConfig<any, any>>;
+  connectionInterfaces?: GraphQLInterfaceType[];
 }
 export interface GraphQLConnectionDefinitions {
   edgeType: GraphQLObjectType;
--- a/node_modules/graphql-relay/connection/connection.js	1985-10-26 09:15:00
+++ b/node_modules/graphql-relay/connection/connection.js	2026-04-23 11:20:33
@@ -7,6 +7,7 @@
 exports.connectionArgs =
   exports.backwardConnectionArgs =
   exports.forwardConnectionArgs =
+  exports.PageInfoType =
     void 0;
 var _graphql = require('graphql');
 /**
@@ -71,9 +72,10 @@
     (_config$name = config.name) !== null && _config$name !== void 0
       ? _config$name
       : (0, _graphql.getNamedType)(nodeType).name;
-  const edgeType = new _graphql.GraphQLObjectType({
+  const edgeType = config.edgeType || new _graphql.GraphQLObjectType({
     name: name + 'Edge',
     description: 'An edge in a connection.',
+    interfaces: config.edgeInterfaces,
     fields: () => {
       var _config$edgeFields;
       return {
@@ -99,6 +101,7 @@
   const connectionType = new _graphql.GraphQLObjectType({
     name: name + 'Connection',
     description: 'A connection to a list of items.',
+    interfaces: config.connectionInterfaces,
     fields: () => {
       var _config$connectionFie;
       return {
@@ -136,7 +139,7 @@
 /**
  * The common page info type used by all connections.
  */
-const pageInfoType = new _graphql.GraphQLObjectType({
+const pageInfoType = exports.PageInfoType = new _graphql.GraphQLObjectType({
   name: 'PageInfo',
   description: 'Information about pagination in a connection.',
   fields: () => ({
--- a/node_modules/graphql-relay/index.d.ts	1985-10-26 09:15:00
+++ b/node_modules/graphql-relay/index.d.ts	2026-04-23 11:21:46
@@ -12,6 +12,7 @@
   connectionArgs,
   connectionDefinitions,
   forwardConnectionArgs,
+  PageInfoType,
 } from './connection/connection';
 export {
   connectionFromArray,
--- a/node_modules/graphql-relay/index.js	1985-10-26 09:15:00
+++ b/node_modules/graphql-relay/index.js	2026-04-23 11:20:42
@@ -27,6 +27,12 @@
     return _connection.forwardConnectionArgs;
   },
 });
+Object.defineProperty(exports, 'PageInfoType', {
+  enumerable: true,
+  get: function () {
+    return _connection.PageInfoType;
+  },
+});
 Object.defineProperty(exports, 'connectionFromArray', {
   enumerable: true,
   get: function () {
